import React, { Component } from 'react';
import { Typography } from '@material-ui/core';

class Profile extends Component {
    render(){
        return(
            <Typography variant="h6" component="h6">Profile Page</Typography>
        )
    }
}

export default Profile;